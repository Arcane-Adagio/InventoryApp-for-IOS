//
//  oldCodeSavedForLater.swift
//  InventoryApp
//
//  Created by Patrick Coleman on 2/28/23.
//

import Foundation

/** To create a custome overlay over an item incase I stop using the built-in list*/
//        .overlay(
//            RoundedRectangle(cornerRadius: 10)
//                .stroke()
//        )
//        .padding(
//            EdgeInsets(
//                top: 2,
//                leading: 2,
//                bottom: 2,
//                trailing: 2
//            )
//        )
//        .padding(.horizontal, 15)

/** For customizable text */
//                Text("Create Account")
//                    .font(.system(size: 36, weight: .bold, design: .monospaced))
//                    .padding(.top, 30)

/** From OfflineInventoryView */
//    @FetchRequest(
//        sortDescriptors: [NSSortDescriptor(keyPath: \CDInventory.id, ascending: true)],
//        animation: .default)
//
//    @FetchRequest(
//        sortDescriptors: [NSSortDescriptor(keyPath: \CDInventory.name, ascending: true)],
//        animation: .default)

//    private var items: FetchedResults<Item>
//    private var items2: FetchedResults<CDInventory>

/* Replace this implementation with code to handle the error appropriately.
 fatalError() causes the application to generate a crash log and terminate.
 You should not use this function in a shipping application,
 although it may be useful during development. */
//                let nsError = error as NSError
//                fatalError("Unresolved error \(nsError), \(nsError.userInfo)")

/* Replace this implementation with code to handle the error appropriately.
 fatalError() causes the application to generate a crash log and terminate.
You should not use this function in a shipping application,
although it may be useful during development. */
//                let nsError = error as NSError
//                fatalError("Unresolved error \(nsError), \(nsError.userInfo)")

/** From ContentView */
//            .toolbar {
//                ToolbarItem(placement: .navigationBarLeading) {
//                    Text("Offline Inventories").font(.headline)
//                }
//                ToolbarItem {
//                    Button(action: showOptions) {
//                        Image(systemName: "ellipsis")
//                            .rotationEffect(Angle(degrees: 90))
//                            .foregroundColor(Color.white)
//                            .shadow(radius: 1)
//                            .overlay(showMoreOptions ? optionsOverlay: nil)
//                    }
//                }
//            }
